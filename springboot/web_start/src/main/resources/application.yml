server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test

project:
  folder: E:E/ProjectFile/springboot/web_start

log:
  root:
    level: debug